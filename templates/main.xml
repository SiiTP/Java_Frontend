<?xml version="1.0"?>
<fest:template xmlns:fest="http://fest.mail.ru" context_name="data">
    <fest:set name="LoggedBar">
        <div class="logout">
            <div class="logout__info">Вы вошли, как:<fest:space/><span><fest:value>data.name</fest:value></span></div>
            ;
            <fest:space/>
            <div class="logout__info">Ваш счет :<fest:space/><span><fest:value>data.score</fest:value></span></div><br/>
            <button type="button" class="button button_logout" data-href="#logout">Logout</button>
        </div>
    </fest:set>

    <div class="menu-container">
        <div class="header">
            <div class="logo">
                <div class="logo__top">
                    <div class="logo__top__line"> </div>
                </div>
                <div align="center" class="logo__center">
                    <span class="logo__beam">Beam</span>
                    <span class="logo__divider"> </span>
                    <span class="logo__balls">Balls</span>
                </div>
                <div class="logo__bottom">
                    <div class="logo__bottom__line"> </div>
                </div>
            </div>
            <div class="vertical-line vertical-line_headerSmall"> </div>
            <fest:choose>
                <fest:when test="data.isLogged">
                    <fest:get name="LoggedBar"/>
                </fest:when>
                <fest:when test="!data.isLogged">
                    <div class="dontLogged">Вы не авторизованы</div>
                </fest:when>
            </fest:choose>
        </div>
        <div align="center" class="menu">
            <div class="container-game-btn">
                <button class="button container-game-btn__button container-game-btn__button_game
                {data.isLogged == true ? '' : 'container-game-btn__button_disabled'}" data-href="#rooms">Game</button>
                <label class="container-game-btn__msg"> </label>
            </div>
            <div class="container-game-btn">
                <button class="button container-game-btn__button container-game-btn__button_scoreboard" data-href="#scoreboard">Scoreboard</button>
            </div>
            <div class="container-game-btn">
                <fest:choose>
                    <fest:when test="data.isLogged">
                        <button class="button container-game-btn__button container-game-btn__button_log" data-href="#logout">Logout</button>
                    </fest:when>
                    <fest:when test="!data.isLogged">
                        <button class="button container-game-btn__button container-game-btn__button_log" data-href="#login">Login</button>
                    </fest:when>
                </fest:choose>
            </div>
        </div>
        <div class="line line_blue"> </div>
    </div>
</fest:template>
